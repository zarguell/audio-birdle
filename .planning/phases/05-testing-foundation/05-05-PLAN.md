---
phase: 05-testing-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01", "02", "03", "04"]
files_modified: [tests/integration/README.md, .planning/codebase/TESTING.md, .planning/STATE.md, vitest.config.js]
autonomous: true
user_setup: []

# Goal-backward verification
must_haves:
  truths:
    - "Testing patterns are documented for future developers"
    - "Test coverage meets or exceeds threshold"
    - "Integration test approach is clear and repeatable"
    - "Phase 5 completion is documented in STATE.md"
  artifacts:
    - path: "tests/integration/README.md"
      provides: "Integration testing guide"
      contains: "new test patterns, fixture usage, error scenarios"
    - path: ".planning/codebase/TESTING.md"
      provides: "Updated testing documentation"
      contains: "integration test patterns, coverage improvements"
    - path: ".planning/STATE.md"
      provides: "Project state updated with Phase 5 completion"
      contains: "Phase 5: Testing Foundation - complete"
  key_links:
    - from: "tests/integration/*.test.jsx"
      to: "tests/integration/README.md"
      via: "documentation links test files"
      pattern: "See.*integration.*test.*files"
    - from: "vitest.config.js"
      to: "tests/integration/README.md"
      via: "configuration documented"
      pattern: "vitest.*config"
---

<objective>
Document testing patterns and update test coverage tracking.

Purpose: Ensure testing knowledge is captured for future developers and test coverage is properly tracked. This final plan completes Phase 5 by documenting the integration testing approach, updating test documentation, and verifying coverage targets are met.

Output: Updated integration testing guide, refreshed testing documentation, updated project state, and verified coverage metrics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/05-testing-foundation/05-01-DISCOVERY.md
@tests/integration/README.md
@tests/fixtures/integration-fixtures.jsx
@tests/fixtures/README.md
@vitest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update integration testing guide</name>
  <files>tests/integration/README.md</files>
  <action>
Update tests/integration/README.md with Phase 5 additions:

1. **Add New Test Files Section:**
   - Document audio-playback.test.jsx
   - Document store-interactions.test.jsx
   - Document data-loading.test.jsx
   - Document error-scenarios.test.jsx
   - Document network-failures.test.jsx

2. **Add Fixture Usage Section:**
   - How to import and use fixtures from tests/fixtures/integration-fixtures.jsx
   - Examples of common fixture patterns
   - Link to tests/fixtures/README.md

3. **Add Integration Test Patterns Section:**
   - Workflow testing (load → play → win)
   - Error scenario testing (empty data, malformed data, network failures)
   - State persistence testing (localStorage, migrations)
   - Cross-module testing (multiple utilities working together)

4. **Update Examples:**
   - Add examples from new test files
   - Show how to test error scenarios
   - Demonstrate fixture usage in real tests

5. **Update Running Tests Section:**
   - Add command to run specific integration test files
   - Add command to run all integration tests with coverage
   - Document test timeout configuration

Preserve all existing content, add new sections after current content.
  </action>
  <verify>README.md updated with new test files documented, fixture usage section added, examples updated, no existing content removed</verify>
  <done>Integration testing guide updated with Phase 5 additions</done>
</task>

<task type="auto">
  <name>Task 2: Update codebase testing documentation</name>
  <files>.planning/codebase/TESTING.md</files>
  <action>
Update .planning/codebase/TESTING.md with Phase 5 improvements:

1. **Update Integration Tests Section:**
   - Document new integration test files
   - Add coverage of audio playback, stores, data loading
   - Add coverage of error scenarios and network failures
   - Update test count estimates

2. **Add Fixtures and Factories Section:**
   ```markdown
   ## Fixtures and Factories

   **Location:** `tests/fixtures/integration-fixtures.jsx`

   **Factory Functions:**
   - `createTestBird(overrides)` - Create test bird data
   - `createTestBirdList(count, overrides)` - Create multiple test birds
   - `createTestGameState(overrides)` - Create test game state
   - `createTestDailyGame(overrides)` - Create test daily game
   - `createTestRegion(overrides)` - Create test region data
   - `createTestDailyEntry(overrides)` - Create test daily entry

   **Usage:**
   ```javascript
   import { createTestBird, createTestGameState } from '@/test/fixtures/integration-fixtures'

   const bird = createTestBird({ id: 'custom-bird' })
   const gameState = createTestGameState({ stats: { totalGamesPlayed: 5 } })
   ```

   **Helpers:** `tests/setup.js` provides createMockLocalStorage(), createMockAudio(), createMockResponse()
   ```

3. **Update Coverage Section:**
   - Document current coverage metrics (run npm run test:coverage)
   - List critical files with coverage percentages
   - Document coverage improvements from Phase 5

4. **Add Integration Test Patterns Section:**
   - Workflow testing pattern
   - Error scenario testing pattern
   - State persistence testing pattern
   - Cross-module interaction testing pattern

5. **Update CI Integration Section:**
   - Document how integration tests run in CI
   - Update smart-test.yml patterns if needed (reference only, no file changes)

Update analysis date at bottom to 2026-01-16.
  </action>
  <verify>TESTING.md updated with new sections, test counts updated, fixtures documented, coverage metrics added, date updated</verify>
  <done>Codebase testing documentation updated</done>
</task>

<task type="auto">
  <name>Task 3: Run coverage report and verify thresholds</name>
  <files>vitest.config.js</files>
  <action>
Generate and verify test coverage:

1. **Run Coverage Report:**
   ```bash
   npm run test:coverage
   ```

2. **Review Coverage Metrics:**
   - Check overall coverage percentage
   - Check coverage for critical files (GameLogic, DailyBirdUtils, LoadGameData, stores)
   - Verify all files meet 70% threshold
   - Verify critical files meet 80-85% threshold

3. **Update vitest.config.js if Needed:**
   - If coverage is significantly above threshold, consider raising threshold
   - If any file is below threshold, document gap for Phase 6
   - Don't lower thresholds to pass - gaps should be documented

4. **Document Coverage in Summary:**
   - Record coverage percentages for key files
   - Note any files below threshold
   - Identify gaps for Phase 6 (Bug Discovery)

5. **Test Integration Test Command:**
   ```bash
   npm run test:integration
   ```
   Verify all integration tests pass.

Document findings in task commit message.
  </action>
  <verify>Coverage report generated, thresholds verified (or gaps documented), all integration tests pass</verify>
  <done>Coverage report generated and thresholds verified</done>
</task>

<task type="auto">
  <name>Task 4: Update project state with Phase 5 completion</name>
  <files>.planning/STATE.md</files>
  <action>
Update .planning/STATE.md with Phase 5 completion:

1. **Update Current Position:**
   ```markdown
   Phase: 6 of 8 (Bug Discovery)
   Plans: Phase 6 not yet planned
   Status: 📋 Ready for Phase 6 Planning
   Last activity: 2026-01-16 — Phase 5 verified and complete (5/5 plans, integration tests added)

   Progress: ████████████░░░ 88%
   ```

2. **Add Phase 5 to Recent Achievements:**
   ```markdown
   ## Recent Achievements (Phase 5)

   **Plan 05-01 (Research):**
   - Researched Vitest integration testing patterns
   - Documented fixture strategies and mocking approaches
   - Identified 15+ gaps in integration test coverage
   - Created DISCOVERY.md with actionable guidance

   **Plan 05-02 (Infrastructure):**
   - Created tests/fixtures/integration-fixtures.jsx with reusable factory functions
   - Enhanced tests/setup.js with integration test helpers
   - Updated vitest.config.js for integration test support
   - Created tests/fixtures/README.md with usage examples

   **Plan 05-03 (Core Flow Tests):**
   - Created audio-playback.test.jsx (80+ lines, 8 test cases)
   - Created store-interactions.test.jsx (100+ lines, 10 test cases)
   - Created data-loading.test.jsx (100+ lines, 12 test cases)
   - All tests cover success paths and failure scenarios

   **Plan 05-04 (Edge Case Tests):**
   - Created error-scenarios.test.jsx (100+ lines, 15 test cases)
   - Created network-failures.test.jsx (80+ lines, 12 test cases)
   - Tests cover empty data, malformed data, boundary conditions
   - Tests cover network retries, backoff, timeouts, recovery

   **Plan 05-05 (Documentation):**
   - Updated tests/integration/README.md with new test patterns
   - Updated .planning/codebase/TESTING.md with fixture documentation
   - Verified coverage meets 70% threshold
   - Documented Phase 5 completion
   ```

3. **Update Performance Metrics:**
   Add Phase 5 row to table after planning and execution complete.

4. **Update Accumulated Context:**
   - Add decision: "Integration tests test module interactions without UI components"
   - Add decision: "Reusable fixtures make integration tests efficient to write"
   - Add decision: "Error scenario testing ensures graceful degradation"
   - Remove Phase 5 from blockers/concerns if present

5. **Update Session Continuity:**
   ```markdown
   Last session: 2026-01-16 [time]
   Current session: 2026-01-16 [time]
   Stopped at: Phase 5 complete, integration tests added
   Next: Phase 6 (Bug Discovery)
   ```

6. **Update Recent Achievements Section:**
   Move Phase 4 to "Earlier Achievements" section if needed to keep recent section focused.
  </action>
  <verify>STATE.md updated with Phase 5 completion, progress updated to 88%, Phase 6 set as next, metrics table includes Phase 5</verify>
  <done>Project state updated with Phase 5 completion</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] tests/integration/README.md updated with new test patterns
- [ ] .planning/codebase/TESTING.md updated with fixture documentation
- [ ] Coverage report generated and thresholds verified
- [ ] All integration tests pass (npm run test:integration)
- [ ] STATE.md updated with Phase 5 completion
- [ ] Progress bar shows 88% (5/8 phases complete)
</verification>

<success_criteria>

- Integration testing approach documented for future developers
- Fixtures and helpers are clear and reusable
- Coverage meets or exceeds 70% threshold
- Integration test command passes all tests
- Project state reflects Phase 5 completion
- Ready to proceed to Phase 6 (Bug Discovery)
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-foundation/05-05-SUMMARY.md`
</output>
