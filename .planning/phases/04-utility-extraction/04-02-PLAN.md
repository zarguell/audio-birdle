---
phase: 04-utility-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/StorageUtils.jsx
  - src/utils/AudioUtils.jsx
  - tests/unit/utils/StorageUtils.test.jsx
  - tests/unit/utils/AudioUtils.test.jsx
autonomous: true
domain: utility-extraction

must_haves:
  truths:
    - "Unified storage API handles all localStorage operations"
    - "Error handling is consistent across all storage operations"
    - "AudioUtils uses StorageManager instead of direct localStorage"
    - "StorageUtils provides get/set/remove with error handling"
    - "Tests verify error handling for quota exceeded, disabled storage"
  artifacts:
    - path: "src/utils/StorageUtils.jsx"
      provides: "Unified localStorage operations with error handling"
      exports: ["getStorage", "setStorage", "removeStorage", "isStorageAvailable"]
      min_lines: 60
    - path: "tests/unit/utils/StorageUtils.test.jsx"
      provides: "Storage operations test coverage"
      min_lines: 80
  key_links:
    - from: "src/utils/AudioUtils.jsx"
      to: "src/utils/StorageUtils.jsx"
      via: "import { getStorage, setStorage, removeStorage } from './StorageUtils'"
    - from: "src/utils/CacheUtils.jsx"
      to: "src/utils/StorageUtils.jsx"
      via: "import { isStorageAvailable } from './StorageUtils'"
---

<objective>
Create a unified storage operations utility to eliminate duplicate localStorage error handling patterns and provide consistent, reliable storage operations across the application.

Purpose: Standardize localStorage access, improve error handling, and make storage operations more maintainable and testable.

Output: Enhanced StorageUtils.jsx module with unified API used by AudioUtils and CacheUtils, with comprehensive error handling and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/utils/StorageUtils.jsx
@src/utils/AudioUtils.jsx
@src/utils/CacheUtils.jsx
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance StorageUtils with unified API</name>
  <files>src/utils/StorageUtils.jsx</files>
  <action>
Update src/utils/StorageUtils.jsx to provide a unified API:

Current exports (keep and enhance):
- getStoredData(key, defaultValue) - add quota exceeded error handling
- setStoredData(key, value) - add better error messages
- removeStoredData(key) - add logging for failures

New exports to add:
- getStorage(key, defaultValue) - alias for getStoredData with enhanced error handling
- setStorage(key, value) - alias for setStoredData with quota detection
- removeStorage(key) - alias for removeStoredData
- isStorageAvailable() - move from CacheUtils.jsx, enhance with better detection
- getStorageKeys() - utility to list all keys (optional, for debugging)
- clearStorage() - utility to clear all app data (optional)

Error handling improvements:
- Detect QuotaExceededError specifically
- Log warnings with consistent format: "Failed to [operation] for [key]: [error]"
- Return defaultValue on get operations if storage unavailable
- Return boolean success flag for set/remove operations

Implementation should:
- Maintain backward compatibility (keep old function names)
- Add JSDoc comments for all exports
- Follow existing naming conventions (camelCase)
  </action>
  <verify>File updated with new exports, ESLint passes</verify>
  <done>StorageUtils enhanced with unified API and better error handling</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AudioUtils to use StorageUtils</name>
  <files>src/utils/AudioUtils.jsx</files>
  <action>
Update src/utils/AudioUtils.jsx to use StorageUtils:

Find these functions (around lines 58-96):
- loadDeadAudioUrlsCache()
- saveDeadAudioUrlsCache()
- clearDeadAudioUrlsCache()

Replace manual localStorage operations with StorageUtils calls:
- Import: getStorage, setStorage, removeStorage from './StorageUtils'
- Replace localStorage.getItem with getStorage
- Replace localStorage.setItem with setStorage
- Replace localStorage.removeItem with removeStorage
- Remove try-catch blocks (StorageUtils handles errors)
- Keep the deadAudioUrls Set logic
- Keep error console.warn calls if needed for debugging

Preserve exact behavior:
- Same STORAGE_KEYS constant
- Same cache loading logic (populate Set from stored array)
- Same save logic (convert Set to array before storing)
  </action>
  <verify>npm test -- tests/unit/utils/AudioUtils.test.jsx passes</verify>
  <done>AudioUtils refactored to use StorageUtils, tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Update CacheUtils to use StorageUtils.isStorageAvailable</name>
  <files>src/utils/CacheUtils.jsx</files>
  <action>
Update src/utils/CacheUtils.jsx:

Find isLocalStorageAvailable function (around lines 33-42)
- Import isStorageAvailable from './StorageUtils'
- Remove local isLocalStorageAvailable function
- Replace all calls to isLocalStorageAvailable with isStorageAvailable

Verify that:
- No other localStorage operations need refactoring (CacheUtils uses service worker cache mostly)
- Function behavior matches original
  </action>
  <verify>npm test -- tests/unit/utils/CacheUtils.test.jsx passes</verify>
  <done>CacheUtils updated to use StorageUtils, tests passing</done>
</task>

<task type="auto">
  <name>Task 4: Write comprehensive tests for enhanced StorageUtils</name>
  <files>tests/unit/utils/StorageUtils.test.jsx</files>
  <action>
Update tests/unit/utils/StorageUtils.test.jsx:

Add new tests for enhanced functionality:
- Test getStorage returns value for existing key
- Test getStorage returns defaultValue for missing key
- Test getStorage handles JSON parse errors
- Test setStorage handles QuotaExceededError (mock localStorage)
- Test setStorage returns success boolean
- Test removeStorage removes key successfully
- Test isStorageAvailable returns true when localStorage works
- Test isStorageAvailable returns false when localStorage disabled
- Test error messages are logged correctly (spy on console.warn)
- Test new unified API (getStorage, setStorage, removeStorage)

Maintain existing tests for backward compatibility:
- Keep tests for getStoredData, setStoredData, removeStoredData
- Verify old and new APIs work identically

Use vi.mock for localStorage to test error conditions.
  </action>
  <verify>npm test -- tests/unit/utils/StorageUtils.test.jsx passes with 15+ tests</verify>
  <done>Comprehensive test coverage for StorageUtils, all tests passing</done>
</task>

<task type="auto">
  <name>Task 5: Update AudioUtils tests to mock StorageUtils</name>
  <files>tests/unit/utils/AudioUtils.test.jsx</files>
  <action>
Update tests/unit/utils/AudioUtils.test.jsx:

Add mock for StorageUtils at top of file:
- vi.mock('./StorageUtils')
- Mock getStorage, setStorage, removeStorage to interact with fake storage
- Or use actual localStorage mock in setup.js

Update tests that verify cache operations:
- Ensure tests still verify cache loading/saving/clearing
- Update any tests that directly check localStorage calls
- Keep all test assertions, only change the mocking approach

Don't change test behavior - only the mock implementation.
  </action>
  <verify>npm test -- tests/unit/utils/AudioUtils.test.jsx passes</verify>
  <done>AudioUtils tests updated to use StorageUtils mocks, all passing</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
Run full test suite:
- npm test -- --run to run all tests once
- Verify 320+ passing tests (maintain or improve pass rate)
- Check storage operations work correctly in all modules
- Verify no new ESLint errors
- Build production version (npm run build)

If test failures occur:
- Investigate storage-related failures
- Ensure mocks are configured correctly
- Verify StorageUtils exports are imported properly
  </action>
  <verify>Full test suite passes, production build succeeds</verify>
  <done>All tests passing, storage operations unified and tested</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] StorageUtils.jsx enhanced with unified API
- [ ] AudioUtils.jsx uses StorageUtils (no direct localStorage)
- [ ] CacheUtils.jsx uses StorageUtils.isStorageAvailable
- [ ] tests/unit/utils/StorageUtils.test.jsx has 15+ tests
- [ ] All tests pass (npm test -- --run)
- [ ] ESLint passes with no new errors
- [ ] Production build succeeds
</verification>

<success_criteria>

- Unified storage API in StorageUtils
- Consistent error handling across all storage operations
- AudioUtils and CacheUtils refactored to use StorageUtils
- All existing tests pass with no regressions
- Code duplication reduced (~30 lines removed from AudioUtils)
  </success_criteria>

<output>
After completion, create `.planning/phases/04-utility-extraction/04-02-SUMMARY.md`
</output>
