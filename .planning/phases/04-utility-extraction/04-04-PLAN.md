---
phase: 04-utility-extraction
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - src/utils/RetryUtils.jsx
  - src/utils/StorageUtils.jsx
  - src/utils/HashUtils.jsx
  - AGENTS.md
autonomous: true
domain: utility-extraction

must_haves:
  truths:
    - "All duplicate retry patterns replaced with RetryUtils"
    - "All duplicate storage patterns replaced with StorageUtils"
    - "Hash implementation is consistent across Python and JavaScript"
    - "Codebase documentation updated with new utility modules"
    - "No remaining duplicate utility patterns identified"
  artifacts:
    - path: "AGENTS.md"
      provides: "Updated codebase documentation"
      contains: "RetryUtils, StorageUtils, enhanced HashUtils"
      min_lines: 300
  key_links:
    - from: "AGENTS.md"
      to: "src/utils/RetryUtils.jsx"
      via: "Documentation section mentions RetryUtils with usage examples"
    - from: "AGENTS.md"
      to: "src/utils/StorageUtils.jsx"
      via: "Documentation section mentions StorageUtils with usage examples"
---

<objective>
Replace all identified duplicate patterns with shared utilities, update documentation, and validate that the codebase has no remaining utility duplications.

Purpose: Complete the utility extraction refactoring by ensuring all duplicate patterns use the new shared utilities, documentation is current, and the codebase is maintainable.

Output: Clean codebase with shared utilities used throughout, updated documentation, and validation that no duplications remain.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/utils/RetryUtils.jsx
@src/utils/StorageUtils.jsx
@src/utils/HashUtils.jsx
@AGENTS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search for remaining duplicate retry patterns</name>
  <files></files>
  <action>
Search codebase for any remaining duplicate retry logic:

Use grep to find retry patterns:

```bash
grep -r "setTimeout.*retry" src/
grep -r "Math.pow(2" src/
grep -r "MAX_RETRIES\|MAX_RETRY" src/
grep -r "attempt.*MAX_RETRY" src/
```

For each match:

- Check if it's already using RetryUtils
- If not, note the file and line number
- Determine if it's a genuine retry pattern or just similar code

Create a list of files that still need refactoring:

- If none found, document that retry logic is fully consolidated
- If found, plan the refactoring for each file

Search scope: src/utils/, src/hooks/, src/components/
Ignore: tests/, .planning/, node_modules/
</action>
<verify>List of remaining retry patterns (or confirmation of none)</verify>
<done>All retry patterns identified and cataloged</done>
</task>

<task type="auto">
  <name>Task 2: Search for remaining duplicate storage patterns</name>
  <files></files>
  <action>
Search codebase for direct localStorage access that should use StorageUtils:

Use grep to find localStorage operations:

```bash
grep -r "localStorage.getItem\|localStorage.setItem\|localStorage.removeItem" src/
grep -r "JSON.parse(localStorage\|JSON.stringify(localStorage" src/
```

For each match (excluding StorageUtils.jsx itself):

- Check if it's using StorageUtils
- If not, note the file and line number
- Determine if it's a one-off access or repeated pattern

Create a list of files that should use StorageUtils:

- Check if error handling is consistent
- Note any files that should be refactored

Search scope: src/utils/, src/hooks/, src/components/
Ignore: tests/, .planning/, node_modules/

Expected findings:

- StorageUtils itself (keep)
- Any remaining direct access in AudioUtils (should be done)
- Any remaining direct access in other utils (refactor if found)
  </action>
  <verify>List of files with direct localStorage access</verify>
  <done>All storage patterns identified and cataloged</done>
  </task>

<task type="auto">
  <name>Task 3: Search for other duplicate utility patterns</name>
  <files></files>
  <action>
Search for other common duplicate patterns:

Error logging patterns:

```bash
grep -r "console.error\|console.warn" src/ | grep -v "test" | head -20
```

- Check if error messages are consistent
- Note any patterns that could be unified

JSON file loading:

```bash
grep -r "fetch.*\.json" src/
grep -r "JSON.parse\|response.json" src/
```

- Check if fetch patterns could use RetryUtils
- Note any manual error handling that's duplicated

Service worker/cache operations:

```bash
grep -r "caches\.\|serviceWorker\|navigator.serviceWorker" src/
```

- Check if cache operations are duplicated

Data transformation/formatting:

```bash
grep -r "toISOString\|Date(" src/
grep -r "toLowerCase\|toUpperCase" src/
```

- Note any repeated formatting patterns

For each pattern category found:

- Document if it's a genuine duplication
- Assess if refactoring is worth the effort
- Create a prioritized list (high/medium/low value)

Stop searching if patterns are too diverse or specific to individual features.
</action>
<verify>List of other potential duplicate patterns with priority ratings</verify>
<done>Other duplicate patterns cataloged and prioritized</done>
</task>

<task type="auto">
  <name>Task 4: Refactor any remaining critical duplications</name>
  <files></files>
  <action>
Based on findings from Tasks 1-3, refactor critical duplications:

Priority 1 (Must fix):

- Any retry patterns not using RetryUtils (refactor immediately)
- Any storage operations with poor error handling (use StorageUtils)

For each file identified:

- Import the appropriate utility (RetryUtils or StorageUtils)
- Replace duplicated code with utility calls
- Test the changes (run related tests)
- Commit in small, focused chunks

Priority 2 (Should fix if time permits):

- Consistent error logging patterns (could create LoggingUtils)
- Common JSON fetching patterns (use RetryUtils)

For each file:

- Assess benefit vs effort
- Refactor if high value and low risk
- Document why if skipped

Priority 3 (Nice to have):

- Minor formatting duplications
- Specific feature patterns (not truly duplications)

Skip these for now - can be addressed in future phases.

Stop when all Priority 1 items are complete.
</action>
<verify>Priority 1 duplications refactored, tests passing</verify>
<done>Critical duplications eliminated, utilities used consistently</done>
</task>

<task type="auto">
  <name>Task 5: Update AGENTS.md with new utility modules</name>
  <files>AGENTS.md</files>
  <action>
Update AGENTS.md to reflect new utility structure:

Find the "Project Structure" section and update:

Under src/utils/ section:

- Add RetryUtils.jsx entry with description: "Shared retry logic with exponential backoff"
- Update StorageUtils.jsx description: "Unified localStorage operations with error handling"
- Update HashUtils.jsx description: "Canonical hash implementation (verified with Python)"
- Ensure each utility lists its main exports

Under "Common Utilities" or similar section:

- Add usage examples for RetryUtils
- Add usage examples for StorageUtils
- Document hash algorithm consistency requirement

Update "Key Data Structures" or "Important Notes":

- Document that retry logic uses RetryUtils (don't reimplement)
- Document that storage operations use StorageUtils (don't use localStorage directly)
- Document hash algorithm and consistency requirement

Update "Development Workflow" section:

- Add guidance: "When adding new features, use RetryUtils for network operations"
- Add guidance: "Use StorageUtils for all localStorage access"

Verify all links and references are correct.
</action>
<verify>AGENTS.md updated with new utilities, documentation complete</verify>
<done>AGENTS.md updated to reflect utility extraction changes</done>
</task>

<task type="auto">
  <name>Task 6: Run final validation and create completion summary</name>
  <files></files>
  <action>
Run comprehensive validation:

Test suite:

- npm test -- --run (full test suite)
- Verify 320+ tests passing (maintain or improve pass rate)
- Check no new test failures introduced

Linting:

- npm run lint
- Verify 0 errors (acceptable warnings only)

Build:

- npm run build
- Verify production build succeeds
- Check console for warnings

Manual validation:

- Start dev server (npm run dev)
- Test game loads correctly
- Test daily bird selection works
- Test audio playback works
- Test settings persist
- Test storage operations work

Documentation validation:

- Review AGENTS.md changes
- Verify all new utilities documented
- Verify usage examples are clear

Create completion checklist:

- [ ] All retry patterns use RetryUtils
- [ ] All storage patterns use StorageUtils
- [ ] Hash implementations verified consistent
- [ ] No critical duplications remain
- [ ] All tests passing
- [ ] Documentation updated
- [ ] Build succeeds

If validation fails:

- Fix issues before declaring complete
- Re-run tests until all pass
  </action>
  <verify>All validation checks pass, Phase 4 complete</verify>
  <done>Phase 4 complete, all utilities extracted and validated</done>
  </task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No critical duplicate retry patterns remain
- [ ] No critical duplicate storage patterns remain
- [ ] Hash consistency verified between Python and JavaScript
- [ ] AGENTS.md updated with new utility modules
- [ ] Full test suite passes (npm test -- --run)
- [ ] Production build succeeds (npm run build)
- [ ] Manual testing confirms no regressions
- [ ] Phase 4 completion checklist validated
</verification>

<success_criteria>

- All critical duplicate patterns eliminated
- Shared utilities used consistently across codebase
- Documentation updated to reflect new structure
- All tests passing with no regressions
- Codebase is more maintainable with reduced duplication
  </success_criteria>

<output>
After completion, create `.planning/phases/04-utility-extraction/04-04-SUMMARY.md`
</output>
