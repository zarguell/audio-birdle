---
phase: 04-utility-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/RetryUtils.jsx
  - src/utils/LoadGameData.jsx
  - src/utils/DailyBirdUtils.jsx
  - tests/unit/utils/RetryUtils.test.jsx
  - tests/unit/utils/LoadGameData.test.jsx
  - tests/unit/utils/DailyBirdUtils.test.jsx
autonomous: true
domain: utility-extraction

must_haves:
  truths:
    - "Retry logic exists in single shared module"
    - "LoadGameData uses RetryUtils for all fetch operations"
    - "DailyBirdUtils uses RetryUtils for all fetch operations"
    - "Retry behavior is consistent across all network operations"
    - "Tests verify retry logic with exponential backoff"
  artifacts:
    - path: "src/utils/RetryUtils.jsx"
      provides: "Shared retry logic with exponential backoff"
      exports: ["fetchWithRetry", "retryWithBackoff"]
      min_lines: 40
    - path: "tests/unit/utils/RetryUtils.test.jsx"
      provides: "Retry logic test coverage"
      min_lines: 50
  key_links:
    - from: "src/utils/LoadGameData.jsx"
      to: "src/utils/RetryUtils.jsx"
      via: "import { fetchWithRetry } from './RetryUtils'"
    - from: "src/utils/DailyBirdUtils.jsx"
      to: "src/utils/RetryUtils.jsx"
      via: "import { fetchWithRetry } from './RetryUtils'"
---

<objective>
Extract duplicate retry logic into a shared RetryUtils module to eliminate code duplication and ensure consistent retry behavior across all network operations.

Purpose: Reduce code duplication, standardize error handling, and make retry behavior configurable and testable.

Output: Shared RetryUtils.jsx module used by LoadGameData and DailyBirdUtils, with comprehensive test coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/utils/LoadGameData.jsx
@src/utils/DailyBirdUtils.jsx
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RetryUtils module with configurable retry logic</name>
  <files>src/utils/RetryUtils.jsx</files>
  <action>
Create src/utils/RetryUtils.jsx with:
- Export fetchWithRetry function (async, with url, options, config parameters)
- Export retryWithBackoff function (generic retry for any async operation)
- Configurable maxRetries (default: 3)
- Configurable baseDelay (default: 1000ms)
- Exponential backoff calculation: delayMs = baseDelay * Math.pow(2, attempt - 1)
- Consistent error logging: console.warn for retries, console.error for final failure
- Throw error after all retries exhausted
- Follow existing code conventions from LoadGameData.jsx and DailyBirdUtils.jsx

Implementation should:

- Use recursive pattern (like LoadGameData) for cleaner code
- Accept config object for easy override of defaults
- Return the response/data on success
- Log retry attempts with context (URL, attempt number, delay, error message)
  </action>
  <verify>File exists at src/utils/RetryUtils.jsx with exports, TypeScript/ESLint passes</verify>
  <done>RetryUtils module created with fetchWithRetry and retryWithBackoff exports</done>
  </task>

<task type="auto">
  <name>Task 2: Write comprehensive tests for RetryUtils</name>
  <files>tests/unit/utils/RetryUtils.test.jsx</files>
  <action>
Create tests/unit/utils/RetryUtils.test.jsx with:
- Test successful fetch (no retries needed)
- Test retry on network failure (verify exponential backoff)
- Test retry on HTTP error status (4xx, 5xx)
- Test max retries exhausted (throws error)
- Test custom config (different maxRetries, baseDelay)
- Test delay calculation with mock timers (vi.useFakeTimers)
- Test error logging with console.warn spy
- Minimum 8 test cases covering happy path and edge cases

Use vi.mock for fetch to simulate failures and successes.
</action>
<verify>npm test -- tests/unit/utils/RetryUtils.test.jsx passes with 8+ tests</verify>
<done>Comprehensive test coverage for retry logic, all tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Refactor LoadGameData to use RetryUtils</name>
  <files>src/utils/LoadGameData.jsx</files>
  <action>
Update src/utils/LoadGameData.jsx:
- Import fetchWithRetry from './RetryUtils'
- Remove existing fetchWithRetry function (lines 3-38)
- Replace all fetch calls with fetchWithRetry calls
- Preserve existing behavior (same defaults, same error handling)
- Keep the loadGameData and refreshGameData functions
- Update imports if needed

Verify that:

- All network operations use RetryUtils
- Default behavior matches original (MAX_RETRIES: 3, RETRY_DELAY_MS: 1000)
- Error messages remain the same for consistency
  </action>
  <verify>npm test -- tests/unit/utils/LoadGameData.test.jsx passes</verify>
  <done>LoadGameData refactored to use RetryUtils, tests passing</done>
  </task>

<task type="auto">
  <name>Task 4: Refactor DailyBirdUtils to use RetryUtils</name>
  <files>src/utils/DailyBirdUtils.jsx</files>
  <action>
Update src/utils/DailyBirdUtils.jsx:
- Import fetchWithRetry from './RetryUtils'
- Remove retry logic from loadDailyBirdData function (lines 39-84)
- Replace manual retry loop with fetchWithRetry call
- Preserve existing behavior with config: { maxRetries: 3, baseDelay: 500 }
- Keep the loadDailyBirdData function structure
- Update loadDailyBirdData to use fetchWithRetry('/data/daily.json', {}, { maxRetries: 3, baseDelay: 500 })

Verify that:

- Daily bird loading uses RetryUtils
- Original retry behavior preserved (3 retries, 500ms base delay)
- Error messages remain consistent
  </action>
  <verify>npm test -- tests/unit/utils/DailyBirdUtils.test.jsx passes</verify>
  <done>DailyBirdUtils refactored to use RetryUtils, tests passing</done>
  </task>

<task type="auto">
  <name>Task 5: Update LoadGameData and DailyBirdUtils tests</name>
  <files>tests/unit/utils/LoadGameData.test.jsx, tests/unit/utils/DailyBirdUtils.test.jsx</files>
  <action>
Update existing tests to mock RetryUtils instead of internal retry logic:
- Add vi.mock for RetryUtils at top of test files
- Mock fetchWithRetry to return test data or throw errors as needed
- Update tests that verify retry behavior to use the mock
- Keep all existing test cases and assertions
- Ensure tests still verify the same behavior (just through the new abstraction)

Don't change test assertions - only the mocking approach.
</action>
<verify>npm test -- tests/unit/utils/LoadGameData.test.jsx tests/unit/utils/DailyBirdUtils.test.jsx pass</verify>
<done>Tests updated to use RetryUtils mocks, all tests passing</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
Run full test suite to ensure no regressions:
- npm test -- --run to run all tests once
- Verify 319+ passing tests (maintain current pass rate)
- Check that no new failures introduced
- Verify retry behavior works correctly in integration

If failures occur:

- Investigate and fix issues
- Ensure RetryUtils is correctly imported and used
- Verify config parameters are passed correctly
  </action>
  <verify>Full test suite passes with same or better pass rate</verify>
  <done>All tests passing, no regressions, retry logic centralized</done>
  </task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] src/utils/RetryUtils.jsx exists with exports
- [ ] tests/unit/utils/RetryUtils.test.jsx exists with 8+ tests
- [ ] LoadGameData.jsx uses RetryUtils (no internal retry logic)
- [ ] DailyBirdUtils.jsx uses RetryUtils (no internal retry logic)
- [ ] All tests pass (npm test -- --run)
- [ ] ESLint passes with no new errors
- [ ] Production build succeeds (npm run build)
</verification>

<success_criteria>

- RetryUtils module created and tested
- LoadGameData and DailyBirdUtils refactored to use RetryUtils
- All existing tests pass with no regressions
- Retry behavior is consistent across all network operations
- Code duplication reduced (~50 lines removed from each file)
  </success_criteria>

<output>
After completion, create `.planning/phases/04-utility-extraction/04-01-SUMMARY.md`
</output>
