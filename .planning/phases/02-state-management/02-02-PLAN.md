---
phase: 02-state-management
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: [src/stores/normalGameStore.ts, src/stores/hardModeStore.ts, src/stores/practiceStore.ts, tests/unit/stores/*.test.ts]
autonomous: true
---

# <objective>

Verify and complete Zustand store implementation with all required state structures and actions.

**Purpose**: Ensure all three stores (normal, hard, practice) have complete state management capabilities matching the existing localStorage-based implementation.

**Output**: Fully functional Zustand stores with comprehensive test coverage.

# <execution_context>

@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

# <context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management/02-01-RESEARCH.md
@.planning/phases/02-state-management/02-01-SUMMARY.md

@src/stores/normalGameStore.ts
@src/stores/hardModeStore.ts
@src/stores/practiceStore.ts
@src/utils/GameLogic.jsx

# <tasks>

<task type="auto">
<name>Task 1: Verify normalGameStore completeness</name>
<files>src/stores/normalGameStore.ts, tests/unit/stores/normalGameStore.test.ts</files>
<action>
Review normalGameStore.ts and ensure:
1. All state fields from GameLogic.jsx are present (dailyGames, stats, version)
2. All required actions exist (setDailyGame, getDailyGame, processGuess, updateStats, reset, migrateFromOldFormat)
3. Persist middleware configured with correct storage key ('audio-birdle-normal-game')
4. Migration function handles v1 → v2 state format changes
5. TypeScript types match existing data structures

Compare with src/utils/GameLogic.jsx state structure to ensure parity.
</action>
<verify>
Run tests: npm test -- tests/unit/stores/normalGameStore.test.ts
All tests pass with coverage >80%
</verify>
<done>
normalGameStore has complete state and actions matching GameLogic.jsx requirements
</done>
</task>

<task type="auto">
<name>Task 2: Verify hardModeStore completeness</name>
<files>src/stores/hardModeStore.ts, tests/unit/stores/hardModeStore.test.ts</files>
<action>
Review hardModeStore.ts and ensure:
1. All state fields present (hardModeGames, hardModeStats, version)
2. All required actions exist (setHardModeGame, getHardModeGame, processHardModeGuess, updateHardModeStats, reset, migrateFromOldFormat)
3. Taxonomic score tracking in guesses (order, family, genus matches)
4. Persist middleware with correct storage key ('audio-birdle-hard-mode')
5. Migration function handles v1 → v2 format

Compare with existing hard mode game logic in GameLogic.jsx.
</action>
<verify>
Run tests: npm test -- tests/unit/stores/hardModeStore.test.ts
All tests pass with coverage >80%
</verify>
<done>
hardModeStore has complete state and actions for hard mode gameplay
</done>
</task>

<task type="auto">
<name>Task 3: Verify practiceStore completeness</name>
<files>src/stores/practiceStore.ts, tests/unit/stores/practiceStore.test.ts</files>
<action>
Review practiceStore.ts and ensure:
1. State structure: currentBird, guesses, completed
2. Actions: setCurrentBird, addGuess, reset
3. NO persist middleware (practice mode doesn't save state)
4. Simple state management for unlimited rounds

Compare with PracticeGame.jsx requirements.
</action>
<verify>
Run tests: npm test -- tests/unit/stores/practiceStore.test.ts
All tests pass with coverage >80%
</verify>
<done>
practiceStore correctly implements transient state for practice mode
</done>
</task>

# <verification>

Before declaring plan complete:
- [ ] All store tests pass (normalGameStore, hardModeStore, practiceStore)
- [ ] Test coverage >80% for each store
- [ ] Store state structures match existing GameLogic.jsx state
- [ ] All required actions implemented and tested
- [ ] Persist middleware correctly configured (except practiceStore)
- [ ] Migration functions handle v1 → v2 state format

# <success_criteria>

- All three stores verified complete with parity to existing state management
- Comprehensive test coverage ensures stores work correctly
- Stores ready for game logic migration in Plan 02-03

# <output>

After completion, create `.planning/phases/02-state-management/02-02-SUMMARY.md` with:
- Verification results for each store
- Any gaps found and fixed
- Test coverage metrics
- Readiness assessment for game logic migration
