---
phase: 02-state-management
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  [
    src/utils/GameLogic.jsx,
    src/App.jsx,
    src/components/NormalModeGame.jsx,
    src/utils/HardModeGame.jsx,
    src/utils/PracticeGame.jsx,
  ]
autonomous: true
---

# <objective>

Migrate game logic components to use Zustand stores instead of manual localStorage management.

**Purpose**: Replace direct localStorage reads/writes with Zustand store actions, centralizing state management and eliminating complex state synchronization logic.

**Output**: Game components using Zustand stores for all state operations, localStorage handled only by persist middleware.

# <execution_context>

@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

# <context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management/02-01-RESEARCH.md
@.planning/phases/02-state-management/02-02-SUMMARY.md

@src/stores/normalGameStore.ts
@src/stores/hardModeStore.ts
@src/stores/practiceStore.ts
@src/utils/GameLogic.jsx
@src/App.jsx

# <tasks>

<task type="auto">
<name>Task 1: Refactor GameLogic.jsx to use stores</name>
<files>src/utils/GameLogic.jsx</files>
<action>
Update GameLogic.jsx functions to use Zustand stores instead of direct localStorage:

1. Replace `getGameState()` with `useNormalGameStore.getState().getDailyGame(key)` and `useHardModeStore.getState().getHardModeGame(key)`
2. Replace `saveGameState()` with `setDailyGame()` and `setHardModeGame()` store actions
3. Replace stats calculations with store actions (`updateStats()`, `updateHardModeStats()`)
4. Remove manual localStorage operations (now handled by persist middleware)
5. Keep the migration function as initial setup only

Do NOT remove GameLogic.jsx yet - components still import from it. Export store-wrapped functions that maintain the same API.
</action>
<verify>
Run tests: npm test -- tests/unit/utils/GameLogic.test.jsx
All 31 tests should still pass (GameLogic maintains same API but uses stores internally)
</verify>
<done>
GameLogic.jsx functions delegate to Zustand stores, maintaining backward compatibility
</done>
</task>

<task type="auto">
<name>Task 2: Update App.jsx to use stores directly</name>
<files>src/App.jsx</files>
<action>
Update App.jsx to use store hooks instead of GameLogic functions:

1. Replace `useGameState()` with `useNormalGameStore()` and `useHardModeStore()` hooks
2. Replace function calls like `getGameState(region, date)` with store selectors
3. Replace `saveGameState()` with store actions
4. Update stats loading to use store state directly
5. Ensure initialization still triggers migration from old localStorage format

Keep the dual-write approach: both old localStorage code and new Zustand stores write during migration phase.
</action>
<verify>
Run build: npm run build succeeds
Run dev: npm run dev starts without errors
Check browser console: No localStorage or store errors
</verify>
<done>
App.jsx uses Zustand stores for all state management, old localStorage code still present for dual-write
</done>
</task>

<task type="auto">
<name>Task 3: Update game components to use store hooks</name>
<files>src/components/NormalModeGame.jsx, src/utils/HardModeGame.jsx, src/utils/PracticeGame.jsx</files>
<action>
Update each game component to use appropriate store:

**NormalModeGame.jsx:**

1. Import `useNormalGameStore`
2. Replace state props with store selectors
3. Replace onGuess callbacks with store actions

**HardModeGame.jsx:**

1. Import `useHardModeStore`
2. Replace state props with store selectors
3. Replace guess handling with store actions including taxonomic scoring

**PracticeGame.jsx:**

1. Import `usePracticeStore`
2. Replace state with store selectors
3. Store updates directly instead of callbacks

Each component should receive minimal props (region, date) and fetch everything else from stores.
</action>
<verify>
Run tests: npm test
All tests pass (components work with stores)
Run dev: npm run dev
Play game in browser - normal, hard, and practice modes all work correctly
</verify>
<done>
All game components use Zustand stores for state, props minimized to region/date only
</done>
</task>

# <verification>

Before declaring plan complete:

- [ ] All GameLogic tests pass (API maintained)
- [ ] Build succeeds without TypeScript errors
- [ ] Dev server starts without errors
- [ ] All three game modes work correctly in browser
- [ ] State persists across page refresh (verified by persist middleware)
- [ ] No direct localStorage writes in components (only via stores)
- [ ] Old localStorage code still present for dual-write migration

# <success_criteria>

- Game components use Zustand stores for all state operations
- Manual localStorage management removed from components
- Dual-write approach maintains old localStorage during migration
- All existing tests pass
- Game works correctly in browser (normal, hard, practice modes)

# <output>

After completion, create `.planning/phases/02-state-management/02-03-SUMMARY.md` with:

- Components migrated to use stores
- Any issues encountered and resolved
- Dual-write implementation status
- Test results
- Known limitations or next steps

# <must_haves>

truths:

- "Game state persists across page refreshes"
- "All three game modes work correctly"
- "Stats update when games complete"
- "No localStorage errors in console"

artifacts:

- path: "src/utils/GameLogic.jsx"
  provides: "Store-wrapped game logic functions"
- path: "src/App.jsx"
  provides: "App using Zustand stores for state"
- path: "src/components/NormalModeGame.jsx"
  provides: "Normal mode using normalGameStore"
- path: "src/utils/HardModeGame.jsx"
  provides: "Hard mode using hardModeStore"
- path: "src/utils/PracticeGame.jsx"
  provides: "Practice mode using practiceStore"

key_links:

- from: "src/App.jsx"
  to: "src/stores"
  via: "useNormalGameStore, useHardModeStore hooks"
- from: "GameLogic.jsx"
  to: "src/stores"
  via: "store actions (setDailyGame, processGuess, updateStats)"
- from: "components"
  to: "src/stores"
  via: "component store hooks replacing props drilling"
