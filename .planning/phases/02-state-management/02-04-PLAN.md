---
phase: 02-state-management
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified: [src/stores/normalGameStore.ts, src/stores/hardModeStore.ts, src/App.jsx, src/utils/StorageUtils.jsx]
autonomous: true
---

# <objective>

Implement backward compatibility for existing user data in localStorage.

**Purpose**: Ensure users with existing game progress (v1 or v2 format) can seamlessly continue using the app with Zustand stores without data loss.

**Output**: Working migration system that converts old localStorage data to new Zustand format, preserving all user progress.

# <execution_context>

@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

# <context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management/02-01-RESEARCH.md
@.planning/phases/02-state-management/02-02-SUMMARY.md
@.planning/phases/02-state-management/02-03-SUMMARY.md

@src/stores/normalGameStore.ts
@src/stores/hardModeStore.ts
@src/utils/StorageUtils.jsx
@src/utils/GameLogic.jsx

# <tasks>

<task type="auto">
<name>Task 1: Implement v1 → v2 migration in stores</name>
<files>src/stores/normalGameStore.ts, src/stores/hardModeStore.ts</files>
<action>
Update the migrateFromOldFormat functions in both stores:

**normalGameStore.ts:**
1. Read old localStorage key: `audio-birdle-game-state`
2. Parse old v1 format (single region, nested structure)
3. Transform to v2 format (multi-region, flat dailyGames object)
4. Handle old stats structure → new regionStats structure
5. Set version to 2 after migration
6. Delete old localStorage key after successful migration

**hardModeStore.ts:**
1. Read old localStorage key: `audio-birdle-hard-mode`
2. Parse old v1 format
3. Transform to v2 format matching new structure
4. Update stats to include regionStats
5. Set version to 2
6. Clean up old localStorage

Use the migration pattern from RESEARCH.md section "Migration Strategy".
</action>
<verify>
Create test localStorage with v1 data
Load app in browser
Check stores: State migrated to v2 format
Check localStorage: Old keys removed, new keys present
</verify>
<done>
Migration functions convert v1 data to v2 format without data loss
</done>
</task>

<task type="auto">
<name>Task 2: Add dual-write compatibility layer</name>
<files>src/App.jsx, src/utils/StorageUtils.jsx</files>
<action>
Implement dual-write approach as specified in 02-01-RESEARCH.md:

**Phase 1 (Weeks 1-2): Both write**
1. Keep existing StorageUtils functions that write to old localStorage keys
2. Zustand persist middleware writes to new keys
3. Both systems write simultaneously, ensuring no data loss

**App.jsx integration:**
1. On mount, call migrateFromOldFormat() from both stores
2. Continue using old localStorage functions alongside store actions
3. Verify both write paths work correctly

**Dual-write verification:**
- After game completes, check both old and new localStorage keys have data
- Compare data structures to ensure consistency
- Log any discrepancies for debugging

This is a temporary measure - after 2 weeks, remove old localStorage writes (Plan for future phase).
</action>
<verify>
Complete a game in each mode (normal, hard)
Check localStorage: Both old keys and new store keys present
Data in both locations matches
Reload page: Game state loads correctly from stores
</verify>
<done>
Dual-write system ensures zero data loss during transition
</done>
</task>

<task type="auto">
<name>Task 3: Add migration testing and edge case handling</name>
<files>tests/unit/stores/normalGameStore.test.ts, tests/unit/stores/hardModeStore.test.ts</files>
<action>
Add comprehensive migration tests:

**Test cases:**
1. Migrate from empty localStorage (no migration needed)
2. Migrate from v1 with single game played
3. Migrate from v1 with multiple games and stats
4. Migrate from v1 with all three regions
5. Migrate corrupt/invalid data (graceful degradation)
6. Verify old localStorage keys deleted after migration
7. Test re-running migration (should be idempotent)

**Edge cases:**
- Missing fields in old data (use defaults)
- Invalid dates (skip or sanitize)
- Negative/inconsistent stats (reset to safe values)
- Partial migration failures (rollback or partial completion)

Ensure migration never throws - wrap in try/catch and log errors.
</action>
<verify>
Run tests: npm test -- tests/unit/stores/
All migration tests pass
Code coverage >90% for migration functions
</verify>
<done>
Migration system handles all edge cases, never loses user data
</done>
</task>

# <verification>

Before declaring plan complete:
- [ ] Migration functions convert v1 to v2 format correctly
- [ ] Dual-write system active (both old and new keys written)
- [ ] All migration edge cases tested
- [ ] Migration is idempotent (can run multiple times safely)
- [ ] Old users can continue playing without data loss
- [ ] New users start with v2 format directly
- [ ] No localStorage errors in any scenario

# <success_criteria>

- Existing users upgrade seamlessly to v2 format
- All game progress preserved during migration
- Dual-write ensures zero data loss
- Comprehensive test coverage for migration paths
- Migration never crashes or loses data (fails gracefully)

# <output>

After completion, create `.planning/phases/02-state-management/02-04-SUMMARY.md` with:
- Migration implementation details
- Test results for all migration scenarios
- Edge cases handled
- Dual-write status
- Recommendations for removing dual-write in future phase
- Any known issues or limitations

# <must_haves>

truths:
  - "Existing users don't lose game progress"
  - "V1 data migrates to v2 format correctly"
  - "Migration works for all three game modes"
  - "App loads without errors for old and new users"

artifacts:
  - path: "src/stores/normalGameStore.ts"
    provides: "V1→V2 migration for normal mode games"
    contains: "migrateFromOldFormat function"
  - path: "src/stores/hardModeStore.ts"
    provides: "V1→V2 migration for hard mode games"
    contains: "migrateFromOldFormat function"
  - path: "src/App.jsx"
    provides: "Migration trigger on app load"
    contains: "migrateFromOldFormat calls"
  - path: "tests/unit/stores/*.test.ts"
    provides: "Migration test coverage"
    exports: "migration test suites"

key_links:
  - from: "App.jsx on mount"
    to: "store migration functions"
    via: "migrateFromOldFormat() calls"
  - from: "migration functions"
    to: "localStorage old keys"
    via: "localStorage.getItem/setItem"
  - from: "migration functions"
    to: "stores"
    via: "store.setState() with transformed data"
