---
phase: 03-large-file-refactor
type: execute
domain: frontend
---

## <objective>

Extract view components from App.jsx to separate UI logic from routing and composition

## <context>

@.planning/phases/03-large-file-refactor/03-01-PLAN.md
@.planning/phases/03-large-file-refactor/03-02-PLAN.md
@.planning/phases/03-large-file-refactor/03-01-ANALYSIS.md

@src/App.jsx (lines 134-796 contain render functions)
@src/components/BirdCompletionCard.jsx (existing component pattern)
@src/utils/HardModeGame.jsx (existing game component)
@src/utils/PracticeGame.jsx (existing game component)

## <tasks>

<task type="manual">
<name>Create RegionSelector component</name>
<files>src/components/RegionSelector.jsx (new file), src/App.jsx</files>
<action>Extract renderRegionSelector function (lines 134-175, ~42 lines) to separate component. Component props: regions array, today date string, onRegionSelect callback, selectedRegion string. Component reads from useNormalGameStore to check if played today. Update App.jsx to import and use RegionSelector component. Test that region selection works and "Played Today" badges display correctly.</action>
<verify>RegionSelector component created. App.jsx uses new component. Region selection works correctly. "Played Today" badges show for completed games. Component is reusable and testable.</done>
</task>

<task type="manual">
<name>Create ModeSelector component</name>
<files>src/components/ModeSelector.jsx (new file), src/App.jsx</files>
<action>Extract renderModeSelector function (lines 178-296, ~119 lines) to separate component. Component props: gameModes array, onModeSelect callback, selectedRegion string. Component displays three game modes (Normal, Hard, Practice) with icons and descriptions. Update App.jsx to import and use ModeSelector component. Test that mode selection works and all modes display correctly.</action>
<verify>ModeSelector component created. App.jsx uses new component. All three game modes display correctly with icons and descriptions. Mode selection works correctly (normal → game, hard → hard mode, practice → practice mode). Color schemes match original design.</done>
</task>

<task type="manual">
<name>Create StatsView component</name>
<files>src/components/StatsView.jsx (new file), src/App.jsx</files>
<action>Extract renderStats function (lines 298-400, ~103 lines) to separate component. Component props: stats object (from useNormalGameStore), selectedRegion string, onBack callback. Component displays player statistics (total games, wins, streaks, averages). Update App.jsx to import and use StatsView component. Test that stats display correctly and back button returns to mode selector.</action>
<verify>StatsView component created. App.jsx uses new component. Statistics display correctly (total games, wins, current streak, max streak, region-specific stats). Back button returns to mode selector. Stats match original display format.</done>
</task>

<task type="manual">
<name>Create SettingsView component</name>
<files>src/components/SettingsView.jsx (new file), src/App.jsx</files>
<action>Extract renderSettings function (lines 402-478, ~77 lines) to separate component. Component props: onBack callback, onResetData callback, onRefreshData callback, refreshingData boolean, hasUpdate boolean. Component displays settings menu (reset data, refresh data, version info). Update App.jsx to import and use SettingsView component. Test that settings options work and back button returns to mode selector.</action>
<verify>SettingsView component created. App.jsx uses new component. Settings options display correctly. Reset data clears all game progress. Refresh data updates game data. Back button returns to mode selector. Version info displays correctly.</done>
</task>

<task type="manual">
<name>Create GameView component</name>
<files>src/components/GameView.jsx (new file), src/App.jsx</files>
<action>Extract renderGame function (lines 480-796, ~317 lines) to separate component. Component props: todaysBird object, currentDailyGame object, answerOptions array, makeGuess function, audioPlayer object, selectedRegion string, onShareResult function, onBack function. Component displays main game view with audio player, guesses, and completion card. Update App.jsx to import and use GameView component. Test that game view displays and works correctly.</action>
<verify>GameView component created. App.jsx uses new component. Audio player works correctly. Guess history displays correctly. Answer options display and work correctly. Completion card shows on win. Share button works correctly. Back button returns to mode selector.</done>
</task>

<task type="auto">
<name>Run tests to verify component extractions</name>
<files>src/App.jsx, src/components/*.jsx</files>
<action>Run npm test to verify all existing tests still pass after component extractions. Check for any regressions in UI rendering, game functionality, or navigation. Fix any test failures caused by component extraction.</action>
<verify>All tests pass after component extractions. No regressions in game functionality. All views render correctly. Navigation between views works correctly.</done>
</task>

<task type="manual">
<name>Create tests for new components</name>
<files>tests/unit/components/RegionSelector.test.jsx (new file), tests/unit/components/ModeSelector.test.jsx (new file), tests/unit/components/StatsView.test.jsx (new file), tests/unit/components/SettingsView.test.jsx (new file), tests/unit/components/GameView.test.jsx (new file)</files>
<action>Create unit tests for each new component. Test components using render from @testing-library/react. mock dependencies (useNormalGameStore, useHardModeStore, todaysBird). Test: 1) Component renders correctly with props, 2) User interactions (buttons, callbacks), 3) Conditional rendering (badges, stats), 4) Integration with stores. Ensure each component has ≥75% coverage.</action>
<verify>All new component tests pass. Each component has ≥75% coverage. Components are well-tested and production-ready. Tests cover all component functionality and edge cases.</done>
</task>

## <verification>

Before declaring plan complete:

- [ ] RegionSelector component created and working
- [ ] ModeSelector component created and working
- [ ] StatsView component created and working
- [ ] SettingsView component created and working
- [ ] GameView component created and working
- [ ] App.jsx updated to use all new components
- [ ] All existing tests pass
- [ ] New component tests created with ≥75% coverage
- [ ] All views render correctly (manual test)
- [ ] Navigation between views works (manual test)
- [ ] Game functionality unchanged (manual test)

## <success_criteria>

- 5 new view components created in src/components/
- App.jsx reduced by ~660 lines (render functions extracted)
- App.jsx now ~100-150 lines (routing + composition only)
- All components have individual test files with ≥75% coverage
- All existing tests pass (no regressions)
- UI rendering unchanged (manual verification)
- Navigation unchanged (manual verification)
- Game functionality unchanged (manual verification)

## <output>

After completion, create `.planning/phases/03-large-file-refactor/03-03-SUMMARY.md` with:

# Phase 3, Plan 3 Summary

**View components extracted from App.jsx**

## Accomplishments

- Created RegionSelector component (42 lines)
- Created ModeSelector component (119 lines)
- Created StatsView component (103 lines)
- Created SettingsView component (77 lines)
- Created GameView component (317 lines)
- Updated App.jsx to use all new components
- App.jsx reduced to ~100-150 lines
- Created tests for all new components

## Files Created

- `src/components/RegionSelector.jsx` - Region selection UI
- `src/components/ModeSelector.jsx` - Game mode selection UI
- `src/components/StatsView.jsx` - Statistics display
- `src/components/SettingsView.jsx` - Settings menu
- `src/components/GameView.jsx` - Main game view
- `tests/unit/components/RegionSelector.test.jsx`
- `tests/unit/components/ModeSelector.test.jsx`
- `tests/unit/components/StatsView.test.jsx`
- `tests/unit/components/SettingsView.test.jsx`
- `tests/unit/components/GameView.test.jsx`

## Files Modified

- `src/App.jsx` - Replaced render functions with components

## Metrics

- App.jsx reduced from ~650 to ~100-150 lines
- 5 new components with ≥75% test coverage
- Total extraction: ~700 lines moved from App.jsx
- All existing tests pass
- No regressions in functionality

## Issues Encountered

[Any issues found and how they were resolved]

## Next Step

**Plan 03-04** — Simplify CacheUtils.jsx structure
