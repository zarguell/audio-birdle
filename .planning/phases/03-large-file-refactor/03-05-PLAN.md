---
phase: 03-large-file-refactor
type: execute
domain: frontend
---

## <objective>

Update imports, run final test suite, and validate large file refactor is complete

## <context>

@.planning/phases/03-large-file-refactor/03-01-PLAN.md
@.planning/phases/03-large-file-refactor/03-02-PLAN.md
@.planning/phases/03-large-file-refactor/03-03-PLAN.md
@.planning/phases/03-large-file-refactor/03-04-PLAN.md

@src/App.jsx (refactored with hooks and components)
@src/components/_.jsx (newly extracted components)
@src/hooks/_.js (newly extracted hooks)
@src/utils/CacheUtils.jsx (simplified)

## <tasks>

<task type="manual">
<name>Update import statements in App.jsx</name>
<files>src/App.jsx</files>
<action>Clean up import statements in App.jsx. Remove unused imports. Organize imports into logical groups (React, third-party, components, hooks, utils). Ensure all new components and hooks are imported correctly. Verify import paths are correct and use appropriate relative paths or absolute paths.</action>
<verify>App.jsx imports are clean and organized. No unused imports. All new components and hooks imported correctly. Import paths are correct and follow project conventions.</done>
</task>

<task type="manual">
<name>Update import statements in extracted components</name>
<files>src/components/*.jsx</files>
<action>Review all extracted components (RegionSelector, ModeSelector, StatsView, SettingsView, GameView). Update import statements in each component. Remove unused imports. Organize imports logically. Ensure all dependencies are imported correctly. Fix any import errors.</action>
<verify>All components have clean, organized imports. No unused imports in any component. All dependencies imported correctly. No import errors.</done>
</task>

<task type="manual">
<name>Update import statements in extracted hooks</name>
<files>src/hooks/*.js</files>
<action>Review all extracted hooks (usePersistence, useGameNavigation, useGameInitialization, useShareResult, useMigration). Update import statements in each hook. Remove unused imports. Organize imports logically. Ensure all dependencies are imported correctly. Fix any import errors.</action>
<verify>All hooks have clean, organized imports. No unused imports in any hook. All dependencies imported correctly. No import errors.</done>
</task>

<task type="auto">
<name>Run full test suite</name>
<files>tests/</files>
<action>Run npm test to execute full test suite. Verify all 157+ existing tests pass. Check for any test failures or warnings. Review test coverage report to ensure coverage hasn't dropped below 70%. Fix any issues found.</action>
<verify>Full test suite passes. All existing tests pass. New tests for hooks and components pass. Test coverage ≥70% (ideally higher due to new tests). No test failures or warnings.</done>
</task>

<task type="auto">
<name>Run ESLint and check for errors</name>
<files>src/</files>
<action>Run npm run lint to check for ESLint errors and warnings. Review all linting issues. Fix any errors or critical warnings. Document any warnings that are intentionally ignored. Ensure code quality standards are met.</action>
<verify>ESLint passes with 0 errors. Warnings reviewed and addressed (or documented as intentional). Code meets project quality standards.</done>
</task>

<task type="auto">
<name>Run production build</name>
<files>package.json, src/</files>
<action>Run npm run build to create production build. Verify build completes successfully. Check for any build errors or warnings. Review build output for issues. Ensure bundle size hasn't increased significantly.</action>
<verify>Production build successful. No build errors or warnings. Bundle size reasonable (no significant increase). Build output clean.</done>
</task>

<task type="manual">
<name>Manual testing - Core functionality</name>
<files>src/App.jsx, src/components/*.jsx</files>
<action>Perform manual testing of core functionality: 1) Region selection, 2) Mode selection, 3) Normal mode gameplay (guesses, win/lose), 4) Hard mode gameplay, 5) Practice mode gameplay, 6) Stats display, 7) Settings options, 8) Share functionality, 9) Audio playback, 10) Persistence across page reload. Test in development mode (npm run dev).</action>
<verify>All core functionality works correctly. No regressions in gameplay. Persistence works. All game modes work. UI displays correctly. User experience unchanged.</done>
</task>

<task type="manual">
<name>Verify file structure and line counts</name>
<files>src/App.jsx, src/utils/CacheUtils.jsx</files>
<action>Check that App.jsx is now ≤200 lines. Verify CacheUtils.jsx is ≤200 lines. Count total files in src/components/ and src/hooks/. Verify all extracted files are in correct locations. Check that file organization matches planned structure from 03-01 analysis.</action>
<verify>App.jsx ≤200 lines. CacheUtils.jsx ≤200 lines. All extracted files in correct locations. File structure matches planned organization. Refactor objectives met.</done>
</task>

<task type="manual">
<name>Update AGENTS.md documentation</name>
<files>AGENTS.md</files>
<action>Update AGENTS.md to reflect new file structure. Document new components in src/components/. Document new hooks in src/hooks/. Update App.jsx description (now ~100-150 lines). Update CacheUtils.jsx description (simplified). Update test coverage information.</action>
<verify>AGENTS.md updated with new file structure. All new components documented. All new hooks documented. App.jsx and CacheUtils.jsx descriptions updated. Documentation is accurate and helpful.</done>
</task>

## <verification>

Before declaring plan complete:

- [ ] All import statements cleaned up
- [ ] Full test suite passes (all existing + new tests)
- [ ] Test coverage ≥70%
- [ ] ESLint passes with 0 errors
- [ ] Production build successful
- [ ] Core functionality verified (manual test)
- [ ] App.jsx ≤200 lines
- [ ] CacheUtils.jsx simplified
- [ ] File structure matches planned organization
- [ ] AGENTS.md updated

## <success_criteria>

- All imports clean and organized
- All tests pass (existing + new)
- Test coverage maintained or improved
- ESLint passes with 0 errors
- Production build successful
- Manual testing confirms no regressions
- App.jsx ≤200 lines (75% reduction from 796)
- CacheUtils.jsx simplified and documented
- File structure is clean and maintainable
- AGENTS.md updated with new structure

## <output>

After completion, create `.planning/phases/03-large-file-refactor/03-05-SUMMARY.md` with:

# Phase 3, Plan 5 Summary

**Large file refactor complete and validated**

## Accomplishments

- Updated and cleaned all import statements
- Full test suite passes (all existing + new tests)
- Production build successful
- Manual testing confirms no regressions
- App.jsx reduced to ≤200 lines (75% reduction)
- CacheUtils.jsx simplified and documented
- File structure is clean and maintainable
- AGENTS.md updated with new structure

## Files Modified

- `src/App.jsx` - Final cleanup and imports
- `src/components/*.jsx` - Import cleanup
- `src/hooks/*.js` - Import cleanup
- `AGENTS.md` - Updated documentation

## Phase 3 Summary

### Deliverables Complete

**Custom Hooks (5):**

- usePersistence - Region and mode persistence
- useGameNavigation - View routing
- useGameInitialization - Daily game setup
- useShareResult - Share functionality
- useMigration - Zustand migration

**Components (5):**

- RegionSelector - Region selection UI
- ModeSelector - Game mode selection UI
- StatsView - Statistics display
- SettingsView - Settings menu
- GameView - Main game view

**Optimized Files:**

- App.jsx - Reduced from 796 to ~150 lines (81% reduction)
- CacheUtils.jsx - Simplified and documented

### Test Results

- All 157+ existing tests pass
- 10 new test files created (5 hooks + 5 components)
- Test coverage maintained or improved

### Metrics

| Metric        | Before    | After     | Change |
| ------------- | --------- | --------- | ------ |
| App.jsx lines | 796       | ~150      | -81%   |
| Total files   | 2         | 17+       | +750%  |
| Avg file size | 489 lines | ~70 lines | -86%   |
| Components    | 0         | 5         | +5     |
| Hooks         | 3         | 8         | +5     |

### Benefits Delivered

1. **Maintainability**: Smaller files easier to understand and modify
2. **Testability**: Isolated components and hooks easier to test
3. **Reusability**: Components and hooks can be reused
4. **Navigation**: Find functionality faster in smaller files
5. **Onboarding**: New developers can understand code quicker
6. **Debugging**: Isolate issues to specific files faster

## Issues Encountered

[Any issues found and how they were resolved]

## Next Step

**Phase 4** — Utility Extraction (create shared utilities)
