---
phase: 03-large-file-refactor
type: execute
domain: frontend
---

## <objective>

Extract custom hooks from App.jsx to separate business logic from UI rendering

## <context>

@.planning/phases/03-large-file-refactor/03-01-PLAN.md
@.planning/phases/03-large-file-refactor/03-01-ANALYSIS.md

@src/App.jsx (lines 30-132 contain hook extraction opportunities)
@src/hooks/useAudioPlayer.js (existing hook pattern)
@src/hooks/useGameData.js (existing hook pattern)
@src/hooks/useDailyGame.js (existing hook pattern)

## <tasks>

<task type="manual">
<name>Create usePersistence hook</name>
<files>src/hooks/usePersistence.js (new file), src/App.jsx</files>
<action>Extract persistence logic from App.jsx lines 32-37. Create usePersistence hook that returns: selectedRegion, setSelectedRegion, lastPlayedMode, setLastPlayedMode. Hook uses getStoredData to initialize state from localStorage. Update App.jsx to use new hook instead of inline useState. Test that region and mode persistence still works correctly.</action>
<verify>usePersistence hook created and tested. App.jsx uses hook for persistence state. Region and mode selection persist across page reloads. Hook follows same pattern as existing hooks (useAudioPlayer, useGameData).</done>
</task>

<task type="manual">
<name>Create useGameNavigation hook</name>
<files>src/hooks/useGameNavigation.js (new file), src/App.jsx</files>
<action>Extract view navigation logic from App.jsx line 85. Create useGameNavigation hook that manages currentView state and provides: currentView, setCurrentView, VIEWS constant. Update App.jsx to use new hook. Test that view switching works correctly (region selector → mode selector → game → stats → settings).</action>
<verify>useGameNavigation hook created. App.jsx uses hook for view state. All view transitions work correctly. Navigation state persists as expected (or not, based on requirements).</done>
</task>

<task type="manual">
<name>Create useGameInitialization hook</name>
<files>src/hooks/useGameInitialization.js (new file), src/App.jsx</files>
<action>Extract game initialization logic from App.jsx lines 117-132. Create useGameInitialization hook that: 1) Initializes daily game if it doesn't exist, 2) Uses selectedRegion, today, and currentDailyGame as dependencies, 3) Calls useNormalGameStore.getState().setDailyGame() for new games. Update App.jsx to use new hook. Test that game initializes correctly for new players and existing players.</action>
<verify>useGameInitialization hook created. App.jsx uses hook for game initialization. New players get fresh game state. Existing players' game state preserved. Game initializes on region change and date change.</done>
</task>

<task type="manual">
<name>Create useShareResult hook</name>
<files>src/hooks/useShareResult.js (new file), src/App.jsx</files>
<action>Extract share functionality from App.jsx lines 88-99. Create useShareResult hook that: 1) Takes currentDailyGame, todaysBird, selectedRegion as dependencies, 2) Returns handleShareResult function, 3) Uses generateShareText and shareResult from ShareUtils. Update App.jsx to use new hook. Test that share functionality works correctly after completing a game.</action>
<verify>useShareResult hook created. App.jsx uses hook for sharing. Share dialog opens correctly with formatted text. Share text includes correct game information (bird name, region, score).</done>
</task>

<task type="manual">
<name>Create useMigration hook</name>
<files>src/hooks/useMigration.js (new file), src/App.jsx</files>
<action>Extract migration logic from App.jsx lines 102-114. Create useMigration hook that: 1) Runs once on app mount, 2) Calls migrateFromOldFormat() on both normalGameStore and hardModeStore, 3) Logs migration status. Update App.jsx to use new hook. Test that migration runs on app load and existing data migrates correctly to v2 format.</action>
<verify>useMigration hook created. App.jsx uses hook for migration. Migration runs once on app mount. Old localStorage data migrates to Zustand v2 format. Console logs show migration status. No data loss during migration.</done>
</task>

<task type="auto">
<name>Run tests to verify hook extractions</name>
<files>src/App.jsx, src/hooks/*.js</files>
<action>Run npm test to verify all existing tests still pass after hook extractions. Check for any regressions in game logic, persistence, or view navigation. Fix any test failures caused by hook extraction.</action>
<verify>All tests pass after hook extractions. No regressions in game functionality. Persistence works correctly. View navigation works correctly. Migration works correctly.</done>
</task>

<task type="manual">
<name>Create tests for new custom hooks</name>
<files>tests/unit/hooks/usePersistence.test.jsx (new file), tests/unit/hooks/useGameNavigation.test.jsx (new file), tests/unit/hooks/useGameInitialization.test.jsx (new file), tests/unit/hooks/useShareResult.test.jsx (new file), tests/unit/hooks/useMigration.test.jsx (new file)</files>
<action>Create unit tests for each new custom hook. Test hooks using renderHook from @testing-library/react. Mock dependencies (getStoredData, useNormalGameStore, ShareUtils). Test: 1) Initial state values, 2) State updates, 3) Side effects (migration, game initialization), 4) Error handling. Ensure each hook has ≥80% coverage.</action>
<verify>All new hook tests pass. Each hook has ≥80% coverage. Hooks are well-tested and production-ready. Tests cover all hook functionality and edge cases.</done>
</task>

## <verification>

Before declaring plan complete:

- [ ] usePersistence hook created and working
- [ ] useGameNavigation hook created and working
- [ ] useGameInitialization hook created and working
- [ ] useShareResult hook created and working
- [ ] useMigration hook created and working
- [ ] App.jsx updated to use all new hooks
- [ ] All existing tests pass
- [ ] New hook tests created with ≥80% coverage
- [ ] Game functionality verified (manual test)
- [ ] Persistence verified (manual test)
- [ ] Migration verified (manual test)

## <success_criteria>

- 5 new custom hooks created in src/hooks/
- App.jsx reduced by ~100-150 lines (hook logic extracted)
- All hooks follow existing hook patterns (useAudioPlayer, useGameData)
- All hooks have individual test files with ≥80% coverage
- All existing tests pass (no regressions)
- Game functionality unchanged (manual verification)
- Persistence unchanged (manual verification)
- Migration unchanged (manual verification)

## <output>

After completion, create `.planning/phases/03-large-file-refactor/03-02-SUMMARY.md` with:

# Phase 3, Plan 2 Summary

**Custom hooks extracted from App.jsx**

## Accomplishments

- Created usePersistence hook (region and mode state)
- Created useGameNavigation hook (view routing)
- Created useGameInitialization hook (daily game setup)
- Created useShareResult hook (social sharing)
- Created useMigration hook (Zustand migration)
- Updated App.jsx to use all new hooks
- Created tests for all new hooks

## Files Created

- `src/hooks/usePersistence.js` - Region and mode persistence
- `src/hooks/useGameNavigation.js` - View routing state
- `src/hooks/useGameInitialization.js` - Daily game initialization
- `src/hooks/useShareResult.js` - Share functionality
- `src/hooks/useMigration.js` - Zustand store migration
- `tests/unit/hooks/usePersistence.test.jsx`
- `tests/unit/hooks/useGameNavigation.test.jsx`
- `tests/unit/hooks/useGameInitialization.test.jsx`
- `tests/unit/hooks/useShareResult.test.jsx`
- `tests/unit/hooks/useMigration.test.jsx`

## Files Modified

- `src/App.jsx` - Replaced inline logic with custom hooks

## Metrics

- App.jsx reduced from 796 to ~650 lines
- 5 new hooks with ≥80% test coverage
- All existing tests pass
- No regressions in functionality

## Issues Encountered

[Any issues found and how they were resolved]

## Next Step

**Plan 03-03** — Extract view components from App.jsx
